apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: artifact-keeper
  namespace: flux-system
spec:
  type: oci                                        # ← OCI mode
  interval: 5m
  url: oci://cloud-build.cav-test.com/cavisson-platform-helm
  secretRef:
    name: artifact-keeper-auth                     # ← secret from Step 1